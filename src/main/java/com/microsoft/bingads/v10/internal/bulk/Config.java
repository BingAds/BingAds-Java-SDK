package com.microsoft.bingads.v10.internal.bulk;

public class Config {
	
	/**
	 * Define the default interval time in milliseconds of status check.
	 * */
    public static final int DEFAULT_STATUS_CHECK_INTERVAL_IN_MS = 5000;
    
    /**
	 * Define the default timeout in milliseconds of HttpClient.
	 * */
    public static final int DEFAULT_HTTPCLIENT_TIMEOUT_IN_MS = 100000;
}
